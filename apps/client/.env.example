DATABASE_URL="postgresql://postgres:postgres@localhost:5432/church_platform?schema=public"

# Which church to serve while developing. In production the church is
# resolved from the request hostname instead.
DEFAULT_CHURCH_SLUG="grace-chapel"
